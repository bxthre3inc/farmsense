# FarmSense OS v1.0 - Python Dependencies
# Ground-up build for Jetson Nano (ARM64)

# Core async
asyncio>=3.4.3
aiohttp>=3.8.0

# Data processing
numpy>=1.21.0
cupy-cuda11x>=11.0.0; platform_machine=='x86_64'  # GPU acceleration (x86 dev)
# cupy-cuda115; platform_machine=='aarch64'  # Uncomment for Jetson ARM64

# Database
sqlite3  # Built-in

# Cryptography
cryptography>=3.4.8

# Modbus for VRI
pymodbus>=2.5.3

# Web framework (API)
fastapi>=0.85.0
uvicorn>=0.18.0
websockets>=10.3

# Configuration
pyyaml>=6.0

# Satellite data (STAC)
pystac>=1.6.0
pystac-client>=0.5.0

# Geo processing
shapely>=1.8.0
pyproj>=3.4.0

# Testing
pytest>=7.1.0
pytest-asyncio>=0.19.0

# Development
black>=22.0.0
mypy>=0.991
